diff --git a/CarCareTracker.csproj b/CarCareTracker.csproj
index 901e621..5920066 100644
--- a/CarCareTracker.csproj
+++ b/CarCareTracker.csproj
@@ -7,6 +7,7 @@
   </PropertyGroup>
 
   <ItemGroup>
+    <PackageReference Include="Microsoft.AspNetCore.DataProtection.Extensions" Version="10.0.0" />
     <PackageReference Include="CsvHelper" Version="33.1.0" />
     <PackageReference Include="LiteDB" Version="5.0.17" />
     <PackageReference Include="MailKit" Version="4.17.0" />
diff --git a/Program.cs b/Program.cs
index 1413f51..535a255 100644
--- a/Program.cs
+++ b/Program.cs
@@ -5,6 +5,7 @@ using CarCareTracker.Logic;
 using CarCareTracker.Middleware;
 using Microsoft.AspNetCore.Authentication;
 using Microsoft.AspNetCore.Authorization;
+using Microsoft.AspNetCore.DataProtection;
 using Microsoft.AspNetCore.Http.Features;
 using Microsoft.AspNetCore.Server.Kestrel.Core;
 using Microsoft.Extensions.FileProviders;
@@ -123,7 +124,9 @@ builder.Services.AddSignalR();
 
 //configure Auth
 builder.Services.AddHttpClient();
-builder.Services.AddDataProtection();
+// Persist DataProtection keys to /data/keys
+builder.Services.AddDataProtection()
+    .PersistKeysToFileSystem(new DirectoryInfo("/data/keys"));
 builder.Services.AddHttpContextAccessor();
 builder.Services.AddAuthentication("AuthN").AddScheme<AuthenticationSchemeOptions, Authen>("AuthN", opts => { });
 builder.Services.AddAuthorization(options =>
@@ -144,6 +147,16 @@ builder.Services.Configure<FormOptions>(options =>
 
 var app = builder.Build();
 
+app.Use(async (context, next) =>
+{
+    var ingressPath = context.Request.Headers["X-Ingress-Path"].FirstOrDefault();
+    if (!string.IsNullOrWhiteSpace(ingressPath))
+    {
+        context.Request.PathBase = new PathString(ingressPath);
+    }
+    await next();
+});
+
 //configure the HTTP request pipeline.
 app.UseExceptionHandler("/Home/Error");
 
diff --git a/Views/Shared/_Layout.cshtml b/Views/Shared/_Layout.cshtml
index 08ab26b..82807f1 100644
--- a/Views/Shared/_Layout.cshtml
+++ b/Views/Shared/_Layout.cshtml
@@ -30,6 +30,10 @@
 }
 <html lang="en" data-bs-theme="@(useDarkMode ? "dark" : "light")">
 <head>
+    <base href="@(Context.Request.PathBase.HasValue ? Context.Request.PathBase.Value + "/" : "/")" />
+    <script>
+        window.BASE_URL = "@(Context.Request.PathBase.HasValue ? Context.Request.PathBase.Value : "")";
+    </script>
     <meta charset="utf-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <meta name="apple-mobile-web-app-title" content="LubeLogger" />
@@ -185,4 +189,4 @@
     <script>
         setThemeBasedOnDevice();
     </script>
-}
\ No newline at end of file
+}
diff --git a/wwwroot/js/garage.js b/wwwroot/js/garage.js
index fe244f9..b27276e 100644
--- a/wwwroot/js/garage.js
+++ b/wwwroot/js/garage.js
@@ -845,5 +845,5 @@ $(function () {
     });
 })
 function goToAdminPanel() {
-    window.location.href = '/Admin';
-}
\ No newline at end of file
+    window.location.href = (window.BASE_URL || '') + '/Admin';
+}
diff --git a/wwwroot/js/login.js b/wwwroot/js/login.js
index 56e08c0..5a53107 100644
--- a/wwwroot/js/login.js
+++ b/wwwroot/js/login.js
@@ -9,7 +9,7 @@
             if (redirectURL.trim() != "") {
                 window.location.href = redirectURL;
             } else {
-                window.location.href = '/Home';
+                window.location.href = (window.BASE_URL || '') + '/Home';
             }
         } else {
             errorToast("Invalid Login Credentials, please try again.");
@@ -24,7 +24,7 @@ function performRegistration() {
     $.post('/Login/Register', { userName: userName, password: userPassword, token: token, emailAddress: userEmail }, function (data) {
         if (data.success) {
             successToast(data.message);
-            setTimeout(function () { window.location.href = '/Login/Index' }, 500);
+            setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Login/Index' }, 500);
         } else {
             errorToast(data.message);
         }
@@ -35,7 +35,7 @@ function requestPasswordReset() {
     $.post('/Login/RequestResetPassword', { userName: userName }, function (data) {
         if (data.success) {
             successToast(data.message);
-            setTimeout(function () { window.location.href = '/Login/Index' }, 500);
+            setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Login/Index' }, 500);
         } else {
             errorToast(data.message);
         }
@@ -48,7 +48,7 @@ function performPasswordReset() {
     $.post('/Login/PerformPasswordReset', { password: userPassword, token: token, emailAddress: userEmail }, function (data) {
         if (data.success) {
             successToast(data.message);
-            setTimeout(function () { window.location.href = '/Login/Index' }, 500);
+            setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Login/Index' }, 500);
         } else {
             errorToast(data.message);
         }
@@ -93,4 +93,4 @@ function sendRegistrationToken() {
             });
         }
     });
-}
\ No newline at end of file
+}
diff --git a/wwwroot/js/settings.js b/wwwroot/js/settings.js
index 80de9bf..29caf15 100644
--- a/wwwroot/js/settings.js
+++ b/wwwroot/js/settings.js
@@ -78,7 +78,7 @@ function updateSettings() {
     $.post('/Home/WriteToSettings', { userConfig: userConfigObject }, function (data) {
         sloader.hide();
         if (data) {
-            setTimeout(function () { window.location.href = '/Home/Index?tab=settings' }, 500);
+            setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Home/Index?tab=settings' }, 500);
         } else {
             errorToast(genericErrorMessage());
         }
@@ -109,7 +109,7 @@ function uploadLanguage(event) {
     }).done((response) => {
         sloader.hide();
         if (response.success) {
-            setTimeout(function () { window.location.href = '/Home/Index?tab=settings' }, 500);
+            setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Home/Index?tab=settings' }, 500);
         } else {
             errorToast(response.message);
         }
@@ -137,7 +137,7 @@ function restoreBackup(event) {
                 if (data) {
                     console.log('LubeLogger - DB Restoration Completed');
                     successToast("Backup Restored");
-                    setTimeout(function () { window.location.href = '/Home/Index' }, 500);
+                    setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Home/Index' }, 500);
                 } else {
                     errorToast(genericErrorMessage());
                     console.log('LubeLogger - DB Restoration Failed - Failed to process backup file.');
@@ -185,7 +185,7 @@ function createAndUploadTranslation(translationName, translationData) {
     }).done((response) => {
         sloader.hide();
         if (response.success) {
-            setTimeout(function () { window.location.href = '/Home/Index?tab=settings' }, 500);
+            setTimeout(function () { window.location.href = (window.BASE_URL || '') + '/Home/Index?tab=settings' }, 500);
         } else {
             errorToast(response.message);
         }
@@ -477,4 +477,4 @@ function loadTooltips() {
         event.stopPropagation();
         event.preventDefault();
     })
-}
\ No newline at end of file
+}
diff --git a/wwwroot/js/shared.js b/wwwroot/js/shared.js
index 32b8bfd..edd3eed 100644
--- a/wwwroot/js/shared.js
+++ b/wwwroot/js/shared.js
@@ -1,4 +1,11 @@
-﻿$.expr.pseudos.containsNC = $.expr.createPseudo(function (arg) {
+﻿$(document).ready(function() {
+    $.ajaxPrefilter(function(options) {
+        if (options.url && options.url.startsWith('/') && window.BASE_URL) {
+            options.url = window.BASE_URL + options.url;
+        }
+    });
+});
+$.expr.pseudos.containsNC = $.expr.createPseudo(function (arg) {
     return function (elem) {
         return $(elem).text().toUpperCase().indexOf(arg.toUpperCase()) >= 0;
     };
@@ -6,7 +13,7 @@
 const mobileScreen = window.matchMedia("(max-width: 575px)");
 var eventHubConn = undefined;
 function returnToGarage() {
-    window.location.href = '/Home';
+    window.location.href = (window.BASE_URL || '') + '/Home';
 }
 function confirmDelete(message, callBack) {
     Swal.fire({
@@ -81,9 +88,9 @@ function warnToast(message) {
 }
 function viewVehicle(vehicleId, tab) {
     if (tab != undefined) {
-        window.location.href = `/Vehicle/Index?vehicleId=${vehicleId}&tab=${tab}`;
+        window.location.href = (window.BASE_URL || '') + `/Vehicle/Index?vehicleId=${vehicleId}&tab=${tab}`;
     } else {
-        window.location.href = `/Vehicle/Index?vehicleId=${vehicleId}`;
+        window.location.href = (window.BASE_URL || '') + `/Vehicle/Index?vehicleId=${vehicleId}`;
     }
 }
 function saveVehicle(isEdit) {
@@ -2299,4 +2306,4 @@ function downloadQR() {
         downloadLink.click();
         URL.revokeObjectURL(url);
     };
-}
\ No newline at end of file
+}
diff --git a/wwwroot/js/vehicle.js b/wwwroot/js/vehicle.js
index 30e8dcd..e7e3b49 100644
--- a/wwwroot/js/vehicle.js
+++ b/wwwroot/js/vehicle.js
@@ -235,7 +235,7 @@ function deleteVehicle(vehicleId) {
         if (result.isConfirmed) {
             $.post('/Vehicle/DeleteVehicle', { vehicleId: vehicleId }, function (data) {
                 if (data.success) {
-                    window.location.href = '/Home';
+                    window.location.href = (window.BASE_URL || '') + '/Home';
                 } else {
                     errorToast(data.message);
                 }
@@ -897,4 +897,4 @@ function setLastOdometer(mileageInputId) {
         }
         $(`#${mileageInputId}`).val(data);
     });
-}
\ No newline at end of file
+}
